import { Component } from "react";

export default class GetData extends Component {

    render() {
        return (
            <body>
            placeholder for data
            </body>
        );
    }
}