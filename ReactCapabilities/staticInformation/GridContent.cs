using ReactCapabilities.Models;

namespace ReactCapabilities.staticInformation
{
    public static class GridContent
    {
        public static GridModel[] grid = new GridModel[]
        {
            new GridModel(0, "col1", "col2", "col3"),
            new GridModel(1, "initial", "value", "something"),
        };
    }
}
